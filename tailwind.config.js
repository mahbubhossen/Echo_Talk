module.exports = {
  darkMode: "class",  // important: enable class-based dark mode
  plugins: [require("daisyui")],
  // daisyui config (optional but recommended)
  daisyui: {
    themes: ["light", "dark"], // you can add your own custom themes here too
  },
};